import {
    ChatBubbleOutline,
    FavoriteBorderOutlined,
    FavoriteOutlined,
    ShareOutlined
} from "@mui/icons-material";
import { Box, Divider, IconButton, Typography, useTheme } from "@mui/material";
import { Friend } from "../../components/Friend";

const PostWidget = ({ _id,
                    userId,
                    firstName,
                    lastName,
                    description,
                    location,
                    occupation,
                    picturePath,
                    userPicture,
                    likes,
                    comments}) => {

                    }
export default PostWidget;